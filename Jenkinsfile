#!groovy
pipeline {
  agent any
  //properties properties: [pipelineTriggers([]), [$class: 'GithubProjectProperty', displayName: 'Jenkins']]
  stages {
    stage('Test') {
      steps {
        echo "hi"
      }
    }
  }
}
